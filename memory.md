# memory.md

메모리(Ram|램, 요즘 그 비싼 거 맞음)는 바이트의 배열로 볼 수 있음. 1MB 의 램이 있다면, 배열은 0 ~ 1MB-1 처럼 인덱스 됨. 인덱스는 주소라고 알려져 있음.

메모리는 프로그램을 저장하기 위해 사용됨; 스택을, 힙(memory allocation)을, 변수를 저장하기 위해.
간단한 CPU와 RAM 설정에선, 프로그램이 0번 인덱스에서 시작하고, 변수들은 프로그램의 끝부분부터 시작하고, 힙은 변수들 끝부터 시작하고, 최상단 메모리부터 밀어넣을수록 아래쪽으로 내려감.

```
메모리 최상단
+----------+
| stack    | # heap 이랑 반대임
| addr 1M  |
+----------+
| heap     | # 좀 더러운 예기지만 "똥이 아래로 내려가면 대참사니까 위로 올라감" 으로 외우면 참 잘 외워지더라구요...
| grows up |
+----------+
| var      |
+----------+
| code     |
| addr 0   |
+----------+
메모리 최하단
```
