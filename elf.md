# ELF(Executable and Linkable Format) 파일과 Loader

* ELF: Object code(어셈블러 또는 컴파일러), 공용 라이브러리, 장치 드라이버, 코어 덤프, 실행 파일의 공통 표준 파일 형식.

컴파일러/어셈블러/링커는 ELF 형식 파일을 생성함. ELF 파일은 여러 부분으로 나눠짐. 일반적인 부분들은
* `.text` (코드)
* `.data` 초기화된 데이터
* `.rodata` 읽기 전용 데이터(상수)
* `.bss` (초기화되지 않은 데이터)
* 다양한 디버깅 정보들
과 같음.

운영채제 프로그램 로더는 ELF 파일을 읽고..
1. .text 구역을 위해 메모리를 할당한 뒤 그 파일의 데이터를 메모리에 로드함.
2. 초기화된 데이터(.data) 를 위해 메모리를 할당하고, 파일의 데이터를 읽어 메모리에 올림.
3. 상수 데이터(.rodata) 를 위해 메모리를 할당하고, 파일의 데이터를 읽어 메모리에 올림.
4. .bss 구역을 위해 메모리를 할당함. 초기화되지 않은 데이터이기 때문에, 할당만 하면 됨.
5. 링커는 중간 객체 파일을 읽고(`.o` 확장자 파일) 최종 실행 가능 형태로 link 함.
각각의 .o 파일은 다른 .o 파일에서 접근 가능한 변수를 선언해도 되고, 다른 .o 파일에서 선언되었을 변수를 접근할 수 있음.
linker 는 최종 출력에 대해 주소들을 고치기 때문에, 코드는 예상한대로 잘 실행됨.
 
## Permissions (Sections and Privileged Instructions)

컴파일러/어셈블러/링커는 일반적으로 코드를 실행 가능하게만 만듬. 요 주소에 뭔가를 저장하려 하면, segfault 를 얻을 수 있음. (ㅘ!)

.data 와 .bss 구역들은 read/write 로 명시되었고, .rodata 는 read-only 로 명시되어 있음.

메모리에 다양한 크기들의 words가 저장될 방식은 CPU의 "endianess"(인디안) 으로 결정됨.
* big endian(빅 인디안) CPU 는 높은 바이트를 먼저 메모리에 넣고, 그 다음으로 높은 바이트를 넣고... 최종적으로 제일 낮은 바이트를 마지막에 넣음.
* little endian(리틀 인디안) CPU는 낮은 바이트를 먼저 넣고... 마지막으로 높은 바이트를 넣음.

CPU는 요런 권한을 강제하기 위한 특별한 기능이 있음. 권한을 무력화하려 하면, segfault 예외가 던져짐.
OS는 프로그램이 실행될 때 요 기능을 준비해두고, 프로그램을 죽인 다음, 프로그램의 core dump 파일을 (아마도, 거의.. 대부분의 확률로)만듬.
core dump 파일은 추후 실패에 대해 포렌식 디버깅/분석시 사용 가능함.

## MMU

현대 OS는 CPU가 MMU(Memory Management Unit)를 통해 메모리의 일부를 실행한 프로그램에 할당함.
MMU 는 메모리의 물리 주소를 프로그램이 보고 사용하는 논리 주소로 매핑함.
이를 통해, 하나의 예를 들자면, CPU는 1MB의 RAM 을 두개의 512K 주소로 나눈 다음 두 프로그램을 돌릴 수 있음.
이 주소 변경을 통해, 각각의 프로그램은 주소 0에서 시작하고, 512K-1 에서 끝나는 512K의 램이 있다고 생각함.

MMU의 사용은 설명보다 더욱 영리하지만, 일단 결과는 같음.
프로그램이 실행된 경우, 코드와 변수들과 스택, 최소한의 힙이 들어갈만한 크기의 RAM을 할당함.
더 많은 스택과 힙이 필요한 경우, OS는 MMU를 통해 물리 매모리를 프로그램 영역으로 추가함.
프로그램은 요구를 통해 자라나게(크기가 커지게) 됨.

우리의 경우(그냥 프로그램) 머신에서 돌아가는 유일한 프로그램이라고 가정할 수 있음. OS가 MMU를 쓰든 말든.. 노오력과 테크닉은 같기 때문임.

### Paging and Swapping

OS는 프로그램을 실행하기 위해 물리 메모리를 MMU에 설정해줘야 함.
MMU에겐 4096 바이트 청크(pages)로 메모리가 할당되게 됨, 이는 MMU 구현에 필요한 것임.

이 방식은 상당히 효율적임, 작은 어셈블리 프로그램의 경우 RAM 몇 메가바이트만(OS는 기본으로 스택 2MB 사용) 필요하기 때문이고, 컴퓨터는 약 RAM이 16기가 있을 것이니..
이 CPU의 효율적인 메모리 할당은 많은 프로그램을 한번에 같이 실행할 수 있게 함.

프로그램이 MMU를 통해 매핑되지 않은 주소를 접근하려는 경우, page fault exception 이 raise 됨. OS 는 요걸 보고 아마 추가 page 를 매핑하여 접근이 성공하도록 함.

시스템 메모리가 부족한 경우, OS는 프로그램 과/또는 OS 그 자체 데이터를 압축하여 추가 RAM 공간을 만듬.
OS 는 압축된 메모리 공간에 있는 프로그램이 실행될 때 그것을 압축 해제해야 하긴 함. MacOS가 이걸 아주 똑똑하게 한다고...

메모리 초과(Out Of Memory, OOM) 상태가 벌어진 경우, OS는 하나 또는 이상의 메모리를 swap 파일/파티션에 "page" 할 수 있고, 이것은 page fault 를 처리하기 위한 공간을 만듬.
디스크에 페이징된 메모리가 있는 프로그램이 실행 예정인 경우(CPU에 의해), 페이징된 메모리를 다시 읽는 과정에서 더 많은 page fault 를 낼 수 있음.
프로그램이 page fault 낸 메모리에 접근할 일이 없을 수 있지만 아무 문제 없음.

OS가 할 수 있는 또 다른 방법은 프로그램 전채(그리고 데이터 까지)를 swap 파일/파티션에 swap out 하는거임.
프로그램이 실행되게 되면, 통째로 다시 메모리로 읽어들여져야 하고(그리고 MMU도 설정도 준비되어야 함), 다른 프로그램을 디스크로 swap 해야 할 수도 있음.
시스템의 빈 메모리가 좀 많이 적은 경우, 응답 속도가 상당히 느려질 수 있음.

마지막으로, 위의 방식(과 다른 개쩌는 방법)을 사용해도 OOM 상태를 벗어날 수 없는 경우라면, 실행중인 프로그램 아무거나 죽임.
좀 사탄들렸지만 어쩌겠음 방법이없는뎅...

스택은 최상단 메모리부터 아래로 내려옴. 스택이 넘치는(overflow) 경우(할당된 메모리보다 더 아래로 내려가게 되는 경우), page fault 가 발생하고 OS는 메모리 맵에 추가 페이지를 할당하여 스택에게 충분한 공간을 선사할 수 있음.

힙은 초기에 쪼매나지만 합당한 크기의 RAM 을 할당받음. `sbrk` syscall 을 통해 확장될 수 있음. 그 유명한 C의 malloc() 임. 지금 뭐 하는지 아주 잘 이해한다는 조건하에 sbrk() 함수를 직접 부를수도 있긴 함.

## Other exceptions

### Segfault

프로그램이 ㄹㅇ 프로그램의 메모리 맵 경계 밖을 임의로 접근하는 경우가 있을 수 있다는 걸 알고 있어야 함.
Paging과 swapping은 이때 발생하지 않고, MMU는 이러한 주소가 프로그램의 메모리 맵에 매핑되지 않도록 설정되어 있음.
page fault 예외를 발생시키지 않고, CPU/MMU는 segfault 예외를 발생시킴. (C 또는 C++ 할때 뭐 하다보면 뜨는 그거)

### Invalid Opcode

프로그램이 어케했는지는 모르지만 올바르지 못한 x64/amd64 명령을 실행하게 되면, 요 예외를 발생시키고 프로그램이 종료됨.
스택에 아무 숫자나 push 하고 반환하게 되면 발생할 수 있음. 프로그램이 그 랜덤한 주소에서 실행하고.. 거기에 뭐가있을줄 어케암?
만약에 랜덤한 숫자/반환이 할당된 주소 공간 밖에서 프로그램을 실행시키게 만드는 경우, segfault 를 얻을 수 있음. (와!)

### General Protection

프로그램이 권한이 필요한 명령어(특권 명령, OS의 커널 모드/Mode 0에서만 실행 가능한 하드웨어 제어 명령어)를 실행하려 하는 경우, 요 예외를 발생시키고 프로그램이 종료됨.
CLI/STI(interrupt 비활성화/활성화, interrupt: 현재 실행 중인 코드를 중단하고 이벤트를 적시에 처리 가능하게 하는 프로세서 요청) 같이 특권 명령이 있고, OS 는 프로그램이 이걸 비활성화하게 시켜버리면 안됨.
이걸 비활성화 하게 냅둬버리면 멀티테스킹이 안 되게 되니까..
