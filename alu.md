# ALU

* ALU(Arithmetic-Logic Unit): 산술 논리 장치, 정수 바이너리 숫자에 대해 산술과 비트 단위 연산을 진행하는 조합 디지털 회로.

회로를 사용해서 두 임의의 메모리 공간을 더하는 것엔 매우 큰 비용이 듬.
백만 * 백만개의 덧샘 회로가 필요하고, 이건 단순히 덧셈에만 사용되는 것임!

수학(덧셈) 기능은 CPU의 ALU(Arithemetric-Logic Unit, 산술 논리 장치)에 구현되어 있음.
CPU는 일반적인 목적에 사용되는 레지스터들을 소량 재공하고, ALU는 덧셈 회로를 제공된 레지스터를 이용하여 구현함.

칩의 레지스터를 ALU가 수학이랑 논리 연산할때 이용하는 (임시)변수라고 생각할 수 있음.
피연산자나 피연산자들을 레디스터로 불러들어야 "수학을 진행" 할 수 있고, 결과값을 메모리의 변수에 저장할 수 있음.

예를 들어, 0x100과 0x200 메모리 주소에 있는 두 숫자를 더해 0x300에 넣는다고 하고, a와 b로 이름지어진 레지스터가 있다고 할 때:
```
load value at 0x100 into a
load value at 0x200 into b
add a and b, leaving result in a
store a at 0x300
```
다음과 같음.

어셈블리 코드는 요런 느낌임! 메모리를 레지스터로 넣기 위한 연산이 필요하고, 레지스터 둘을 더한 뒤, 레지스터를 메모리에 저장하는 것임.
각각의 연산은 CPU의 "opcode" 라고 함. CPU는 byte opcode를 메모리에서 읽고 실행함.
몇몇 로드랑 저장 같은 opcode는 주소와 같이 어디에 저장하고 로드할지의 매개변수가 필요함. 주소는 opcode 바로 다음에 저장되게 됨.
좀 하다 보면, 명령의 크기(opcode + 매개변수)는 각각 명령과 매개변수에 따라 다르다는걸 알게 될 거임.

젤 단순하게 CPU를 보면, 저 프로그램은 4개의 명령으로 이루어져 있음. 로드와 저장 명령은 opcode 1 byte, 주소에 2바이트 더 쓰고..
그리고 덧셈은 opcode 하나만 사용함. (add b to a)

각각의 명령은 연산의 복잡성에 따라 하나 또는 이상의 "clock cycles"(cpu 연산 단위)을 사용함. (CPU가 WORD 단위 로드 사용한다고 가정되어 있음. 그래서 주소 로드에 2 사이클 소모)
로드 명령은
* opcode 로드에 clock cycle 하나
* 각 주소 바이트 로드에 하나씩 총 두 사이클(0x01.. 0x..00),
* RAM에서 매개변수로 지정된 주소로 로드하는데 또 두 사이클 (RAM(0x0100))
총 5 clock cycles 소모됨.
덧셈은 하나, 저장은 또 다섯.
