# bits.md

## binary

0, 1

## bytes

bits 8개
```
76543210
```

## word

2bytes (16bits)
```
5432109876543210
111111
```

dword: 4bytes (32bits) 더블

qword: 8bytes (64bits) 쿼드

word 값 사용할 때 주로 2, 4, 8, 10, 16진법 사용. 8진법은 잘 안쓰지만, 그래도 사용처는 있음

## 진법

2진법(binary): 11010100010

10진법: 매일쓰는 그거

8진법(octal): 0~7까지 샘. 자릿수 증가는 n * 8

무려 리눅스 파일시스템에서 사용함.
```
-rw-r--r--  1 han han 1103 Jul 31 00:58 bits.md
```
`-rw` 요거 3비트 = 8진법. `chmod {숫자} bits.md` 할때 숫자에서 사용하는 진법이 8진법임.
`rw-rw--w-` 로 만들고 싶다면 `chmod 664 bits.md` 하면 됨. 


16진법(hex): 1~15까지 샘. 10부터는 A, B, C, D, E, F. 자릿수 증가는 n * 16

nybble(니블): 4바이트. hex랑 쓰기 좋음.
4바이트는 1~15까지 저장 가능(8 4 2 1) 딱 16진법 자리 하나.

binary를 hex 로 변환할때도 편함.
`00101010b` 있을때, nybble 단위로 나누면, `0010 1010` = `0x2A` 또는 `0a5h` 또는 `$a5`

요렇게 nybble 은 매우 신이며 4바이트로 나누면 모든 binary 를 hex 로 쉽게 변환할 수 있다. 기습숭배

### bit 수의 따른 저장 가능한 수의 범위

```
1 bit 0~1
2 bits: 0~3
3 bits: 0~7
4 bits: 0~15
5 bits: 0~31
```

1 증가시마다, 기존에 저장 가능한 최고로 큰 값 * 2 + 1 형식으로 증가함

## 수학

같은 word 더하는건 쉬움. 100 + 50 = 150

byte 값을 16-bit word 에 더하면?

byte 는 16비트 값이기도 함. 위쪽 8비트가 0일 뿐. 그래서 `0xaa == 0x00aa` 16비트 + 16비트임.

byte 사이즈 값인 255 에 1 더하면? 

결과값 담는곳은 8비트밖에 없음, 그런데 255 + 1 = 256 = 9비트 필요. (`1111 1111b + 1 = 01 0000 0000b`)
아홉번째 비트부턴 무시됨. 그래서, `255 + 1 = 0`

요거는 32bit, 64bit word 전부 통용되는 개념. 초과분은 날린다.

두개 값 곱하는건 두배의 크기의 저장 공간 필요. `255 * 255 = 65025 (0xfe01)` 결과 16비트임. 그래서 word * word 를 dword 에 안 넣으면, 짤려서 `0x01` 나올 수 있음.

## 부울 대수

이거 해야함 졸려서자야지
